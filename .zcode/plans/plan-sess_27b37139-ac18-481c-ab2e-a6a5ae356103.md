Implementation plan — approved. Rendering-layer addition of a Promotion section to the Homepage.

Files: 3 new + 2 edits + 1 migration.

1. `supabase/migrations/030_promotions_placement.sql` (new) — `placement text NOT NULL DEFAULT 'homepage'`, no CHECK (extensible), partial index `WHERE placement='homepage' AND status='published' AND active=true`.
2. `lib/promotions.ts` (edit) — add type-safe `Placement = 'homepage' | (string & {})`; add `placement` to the `Promotion` interface only. No validation/CRUD/admin changes.
3. `lib/homepagePromotions.ts` (new) — `getHomepagePromotions()` via `createAdminClient()` (ISR-safe), projects only the fields the Homepage needs, exact time window via a single nested `.or()`, returns `[]` on error.
4. `components/PromotionSection.tsx` (new) — Server Component receiving fetched data (no client fetch); CSS scroll-snap slider; `PromotionImage` client subcomponent for graceful image fallback; conditional internal/external button with new-tab only when configured; a11y (`<section aria-label>`, alt text, focus-visible). No carousel library.
5. `app/page.tsx` (edit) — parallel `Promise.all` fetch alongside packages; render section between Hero and Exam Sets; hide when empty.
6. `app/globals.css` (edit) — `.hide-scrollbar` utility.

Query (documented): single-param `.or('and(or(start_at.is.null,start_at.lte.NOW),or(end_at.is.null,end_at.gte.NOW))')` with NOW = `new Date().toISOString()`, ordering `priority DESC, display_order ASC, created_at DESC`, `.limit(4)`.

Untouched per spec: Promotion schema (except approved placement), validation, CRUD, admin, Homepage Settings, Featured Packages, homepage_order, summaries, exams, auth, payment, orders. No analytics/tracking/popup/modal/carousel-lib.

Verify: build/tsc/eslint as final step.